# Evaluation Directory

This directory now uses the task-driven evaluation pipeline (v1) as the primary path.

Main entrypoints:

- `eval/build_task_dataset_v1.py`
- `eval/run_task_eval_v1.py`

Reference documentation:

- `docs/testing/task_eval_v1.md`

Notes:

- `eval/reports/` contains runtime artifacts.
- Frozen/legacy evaluation chains are not part of the active workflow.
